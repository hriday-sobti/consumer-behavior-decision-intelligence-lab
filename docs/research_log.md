# Research & Technical Implementation Log

| Date | Topic | Source / Reference | Finding & Constraint | Implementation Decision |
| :--- | :--- | :--- | :--- | :--- |
| 2026-09-24 | Python 3.14 Environment | Python PEP 749 / sys.version | Host machine running Python 3.14.6 x64. Scikit-learn, numpy, pandas, and psycopg wheels installed cleanly. | Built isolated virtual environment (`.venv/`) using Python 3.14. |
| 2026-09-24 | PostgreSQL Storage Engine | EnterpriseDB Portable Binaries | PostgreSQL service not installed globally on host system; Docker not available. EnterpriseDB standalone binaries provide production `pg_ctl`, `initdb`, `psql`. | Deployed local standalone PostgreSQL 16.4 cluster under `tools/pgsql/` and initialized cluster in `pgdata/` on port `54329`. |
| 2026-09-24 | Ingestion & Schema Alignment | UCI ML Repository DOI 10.24432/C5CG6D | Dataset consists of two sheets: `Year 2009-2010` and `Year 2010-2011`. Column naming matches across sheets with slight casing variations. | Built unified ingestion pipeline that reads both sheets, tracks provenance, and caches interim parquet without altering source bytes. |
| 2026-09-24 | Arrow Type Conversion | PyArrow Documentation | `invoice_no` column contains mixed integers and strings with 'C' prefixes, causing PyArrow conversion errors when stored as generic objects. | Explicitly cast object columns (`invoice_no`, `stock_code`, `description`, `country`) to strings prior to Parquet export. |
| 2026-09-24 | Vectorized Snapshots | Pandas Time-Series Groupby Docs | Iterative customer-by-customer monthly state calculation across 5,878 customers and 25 months timed out at 300s. | Re-engineered monthly snapshot builder into vectorized grouped rolling window and lag shifts, executing 98,557 rows in 3.12 seconds. |
| 2026-09-24 | Clustering Model Selection | Scikit-learn Silhouette API | Primary feature set evaluated across $K \in \{3,4,5,6\}$. Silhouette scores: K=3 (0.2749), K=4 (0.2634), K=5 (0.2509), K=6 (0.2626). | Selected $K=3$ according to the documented rule: smallest $K$ with silhouette $\ge 90\%$ of maximum and all cluster shares $\ge 5\%$. |
| 2026-09-24 | Decision Strategy Catalog Schema | PostgreSQL Data DDL | Initial VARCHAR(64) constraints on strategy catalog descriptions caused truncation errors during bulk insertion. | Widened descriptive columns to `VARCHAR(255)` and `TEXT` in `sql/09_opportunity_flags.sql`. |
